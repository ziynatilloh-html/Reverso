export enum MemberType {
  ADMIN = "ADMIN",
  MEMBER = "MEMBER",
}
export enum MemberStatus {
  ACTIVE = "ACTIVE",
  BLOCKED = "BLOCKED",
  DELETED = "DELETED",
}
export enum AuthProvider {
  LOCAL = "LOCAL",
  GOOGLE = "GOOGLE",
}
